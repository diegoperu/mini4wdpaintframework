# Mini4WD Manual SDK — Page Content
# Page: P006 — Mascheratura (Masking)
# SDK Version: 2.4.0
# SOURCE OF TRUTH

page:
  id: "P006"
  name: "Mascheratura"
  version: "1.0.0"
  language: "it"

title: "Guida alla Mascheratura"  # REQUIRED
intro: ""                         # OPTIONAL

# ============================================================
# ZONES — from PROJECT.yaml maskingZones[]
# ============================================================
zones:
  - id: ""                   # REQUIRED — e.g. "M001"
    area: ""                 # REQUIRED — Italian area description
    masking_type: ""         # REQUIRED — nastro|liquido|pretagliato|frisket
    order: 1                 # REQUIRED — application sequence
    notes: ""                # OPTIONAL

# ============================================================
# MASKING SEQUENCE — ordered steps
# ============================================================
sequence:
  - step: 1
    description: ""          # REQUIRED — Italian imperative
    zone_id: ""              # OPTIONAL — reference to zones[].id
    tip: ""                  # OPTIONAL

warnings:
  - ""                       # C008 — e.g. paint bleed risk

tips:
  - ""                       # C009 — e.g. burnish tape edges

callouts:
  - title: ""                # C006 — e.g. "Tipo di nastro"
    body: ""

footer:
  page_id: "P006"
  model_name: ""
  series: ""

component_mapping:
  C001:
    uses: ["title"]
  C002:
    uses: ["footer.page_id", "footer.model_name"]
  C006:
    uses: ["callouts[].title", "callouts[].body"]
  C008:
    uses: ["warnings"]
  C009:
    uses: ["tips", "sequence[].tip"]
  C012:
    uses: []                 # Zoom panels — visual only with caption from notes
  C013:
    uses: ["sequence[].step"]
