# Mini4WD Manual SDK — Page Content
# Page: P004 — Preparazione (Preparation)
# SDK Version: 2.4.0
# SOURCE OF TRUTH

page:
  id: "P004"
  name: "Preparazione"
  version: "1.0.0"
  language: "it"

title: "Preparazione della Superficie"  # REQUIRED
intro: ""                               # OPTIONAL

# ============================================================
# STEPS — from PROJECT.yaml preparationSteps[]
# All descriptions in Italian, imperative form
# ============================================================
steps:
  - id: 1
    title: ""                # REQUIRED — e.g. "Pulizia della carrozzeria"
    description: ""          # REQUIRED — imperative Italian
    duration: ""             # OPTIONAL — e.g. "15 minuti"
    tools: []                # OPTIONAL — tool names
    warning: ""              # OPTIONAL — feeds C008
    tip: ""                  # OPTIONAL — feeds C009

warnings:
  - ""                       # General page-level warnings for C008

tips:
  - ""                       # General page-level tips for C009

footer:
  page_id: "P004"
  model_name: ""
  series: ""

component_mapping:
  C001:
    uses: ["title"]
  C002:
    uses: ["footer.page_id", "footer.model_name"]
  C007:
    uses: []                 # Exploded view — visual only
  C008:
    uses: ["warnings", "steps[].warning"]
  C009:
    uses: ["tips", "steps[].tip"]
  C013:
    uses: ["steps[].id"]
  C014:
    uses: ["steps[].duration"]
