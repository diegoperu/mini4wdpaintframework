# Mini4WD Manual SDK — Page Content
# Page: P007 — Dettagli (Details)
# SDK Version: 2.4.0
# SOURCE OF TRUTH

page:
  id: "P007"
  name: "Dettagli"
  version: "1.0.0"
  language: "it"

title: "Verniciatura dei Dettagli"  # REQUIRED
intro: ""                           # OPTIONAL

# ============================================================
# DETAIL AREAS — from PROJECT.yaml detailAreas[]
# ============================================================
areas:
  - id: ""                   # REQUIRED — e.g. "D001"
    name: ""                 # REQUIRED — Italian area name
    color_id: ""             # REQUIRED — reference to P002 colors[].id
    technique: ""            # REQUIRED — pennello|aerografo|bomboletta
    description: ""          # REQUIRED — Italian imperative instructions
    notes: ""                # OPTIONAL

warnings:
  - ""                       # C008

tips:
  - ""                       # C009

footer:
  page_id: "P007"
  model_name: ""
  series: ""

component_mapping:
  C001:
    uses: ["title"]
  C002:
    uses: ["footer.page_id", "footer.model_name"]
  C008:
    uses: ["warnings"]
  C009:
    uses: ["tips"]
  C012:
    uses: []                 # Close-up zoom panels
  C013:
    uses: ["areas[].id"]
  C015:
    uses: ["areas[].notes"]
