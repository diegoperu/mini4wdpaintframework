# Mini4WD Manual SDK — Page Content
# Page: P005 — Verniciatura (Painting)
# SDK Version: 2.4.0
# SOURCE OF TRUTH

page:
  id: "P005"
  name: "Verniciatura"
  version: "1.0.0"
  language: "it"

title: "Sequenza di Verniciatura"  # REQUIRED
intro: ""                          # OPTIONAL

# ============================================================
# SEQUENCE — from PROJECT.yaml paintSequence[]
# Order matters: paint light before dark
# ============================================================
sequence:
  - step: 1
    color_id: ""             # REQUIRED — matches content.yaml P002 colors[].id
    area: ""                 # REQUIRED — Italian area description
    technique: ""            # REQUIRED — bomboletta|pennello|aerografo
    coats: 1                 # REQUIRED
    drying_time: ""          # REQUIRED — e.g. "30 minuti tra le mani"
    notes: ""                # OPTIONAL

warnings:
  - ""                       # C008 — at minimum: drying time warning

tips:
  - ""                       # C009

footer:
  page_id: "P005"
  model_name: ""
  series: ""

component_mapping:
  C001:
    uses: ["title"]
  C002:
    uses: ["footer.page_id", "footer.model_name"]
  C005:
    uses: ["sequence[].step", "sequence[].area", "sequence[].color_id"]
  C008:
    uses: ["warnings"]
  C009:
    uses: ["tips"]
  C011:
    uses: ["sequence[].color_id"]
  C013:
    uses: ["sequence[].step"]
  C014:
    uses: ["sequence[].drying_time"]
